ROS2 Package Name - robofest

ROS2 Mine Node Name - mine_detector 

$ ros2 run robofest mine_detector

ROS2 Topic Name - timestamp_pipeline, dataype std_msgs/float64

$ ros2 topic list | grep timestamp_pipeline
$ ros2 topic echo timestamp_pipeline
